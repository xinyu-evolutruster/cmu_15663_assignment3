### 15663 Assignment 3

#### Section 1

Run `python bilaterail_filtering.py` and the program will generate $A^{Base}$, $A^{NR}$, $A^{Detail}$, the mask and $A^{Final}$ in order.

#### Section 2

Run `python gradient_domain.py` and the program will generate the re-integrated image and the fused image.

#### Section 3

The code for this section is commented out out in `bilateral_filtering.py` and `python gradient_domain.py`. Uncomment the code to view the final result.